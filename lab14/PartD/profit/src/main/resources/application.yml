spring:
  ai:
    openai:
      chat:
        options:
          model: gpt-4o
      api-key:
  datasource:
    url: jdbc:mysql://localhost:3306/eadb
    username: root
    password: root
    driver-class: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

logging:
  level:
    root: INFO


